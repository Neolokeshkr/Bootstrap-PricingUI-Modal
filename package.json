{
  "name": "npm-implementation",
  "version": "1.0.0",
  "description": "Npm implementaion of the application which is being served using express and nodemon",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "browser-sync": "browser-sync start --proxy \"localhost:3182\" --files \"index.html\""
  },
  "author": "Loki",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
